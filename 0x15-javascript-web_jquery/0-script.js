document.querySelector('header').style.color = '#FF0000';
/*
 * This code first selects the <header> element using document.querySelector(), which searches the HTML document for the first matching element. It then sets the color property of the selected element's style object to the hex code for red (#FF0000), which changes the text color to red.
 */
